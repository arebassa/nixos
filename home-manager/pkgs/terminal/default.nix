{ pkgs, ... }: {
  programs = {
    tmux = {
      enable = true;

    };
    bat = {
      enable = true;

    };
    exa = {
      enable = true;

    };
    jq = {
      enable = true;

    };
    k9s = {
      enable = true;

    };
  };
}
